// utils/handleTokenExpiry.ts
export default function handleTokenExpiry(message: string) {
    alert(message);
    window.location.reload(); // Refreshes the page to redirect the user to the login page

};







